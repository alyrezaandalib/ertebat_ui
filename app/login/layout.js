export const metadata = {
  title: 'ورود به حساب کاربری | ارتباط',
  description: 'سامانه مدیریت مشتری ارتباط',
}

export default function RootLayout({ children }) {
  return <section>{children}</section>;
}
