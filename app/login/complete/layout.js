export const metadata = {
    title: 'تکمیل اطلاعات کاربری | ارتباط',
    description: 'سامانه مدیریت مشتری ارتباط',
}

export default function RootLayout({children}) {
    return <section>{children}</section>

}
