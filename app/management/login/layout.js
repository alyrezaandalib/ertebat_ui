export const metadata = {
    title: "ورود به حساب مدیریت | ارتباط",
    description: "قسمت ورود به حساب کاربری",
};

export default function RootLayout({ children }) {
    return <section>{children}</section>;
}
